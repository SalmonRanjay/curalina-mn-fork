# Agent work packet

## Task

Service: `curalina_recommendation`
Phase: R01 — catalogue audit notebook
Owner: `ml-notebook-engineer`
Reviewer: `ai-ml-lead` (sign-off; this packet produces evidence, not a
verdict)

## Objective

Fill and execute `architecture/notebooks/recommendation/01_catalogue_audit.ipynb`
against the one real workbook ADR-0005 admits for R01
(`attached_assets/All_Four_Hands_and_Moes_Products_Combined
New_1762391396825.xlsx`, md5 `3ad1f5d7e47cca273e3fecd5ede64054`), to the
7-section notebook standard, and write the real (not fake)
`CatalogueImporter` adapter and audit logic this required.

## Context

Required guides:

- `architecture/guides/01_system_architecture.md`
- `architecture/guides/02_local_setup.md`
- `architecture/guides/03_data_contracts.md`
- `architecture/guides/08_engineering_and_tests.md`
- `architecture/guides/09_delivery_gates.md`
- `architecture/guides/11_agentic_implementation_plan.md`

Additional workstream guides:

- `agentic_flow/16_notebook_standard.md` — the 7-section structure this
  notebook follows.
- `.claude/agents/ml-notebook-engineer.md` — role constraints ("a notebook
  cell may contain narrative, a plot, or a call into the package — never
  business logic").
- `ai_services/recommendation/src/curalina_recommendation/ports/catalogue_importer.py`
  — the port's docstring names this exact notebook as the thing that
  unblocks its real implementation.

Input data, fixtures or decisions:

- **`architecture/adr/ADR-0005-catalogue-workbook-provenance-and-oq011.md`
  — the ruling this entire packet executes.** It admits the pinned
  workbook as a **labelled, real, provenance-unconfirmed input for R01
  only**; R03 stays blocked; OQ-009 is untouched. Two conditions are
  mandatory and were followed: every finding is scoped to the pinned file
  + hash, and the notebook's decision record carries the provenance
  caveat in substance, not as a single mention.
- Pinned file: `attached_assets/All_Four_Hands_and_Moes_Products_Combined
  New_1762391396825.xlsx`, md5 `3ad1f5d7e47cca273e3fecd5ede64054`.
- Excluded: `..._1762332689675.xlsx` (byte-different, md5
  `ef5288a5f1a277ac0d67dbbf45d343a0` — a provenance flag recorded in the
  notebook's Inputs section, not silently ignored).
- Mapper file (designer-authored controlled vocabulary):
  `attached_assets/Quiz and Product Mapper file instruction_1762400632880.xlsx`.

## Allowed files

- `architecture/notebooks/recommendation/01_catalogue_audit.ipynb`
- `ai_services/recommendation/src/curalina_recommendation/adapters/xlsx_parsing.py`
  (new)
- `ai_services/recommendation/src/curalina_recommendation/adapters/xlsx_workbook_reader.py`
  (new)
- `ai_services/recommendation/src/curalina_recommendation/adapters/xlsx_catalogue_importer.py`
  (new)
- `ai_services/recommendation/src/curalina_recommendation/application/catalogue_audit_service.py`
  (new)
- `ai_services/recommendation/tests/unit/adapters/test_xlsx_parsing.py`,
  `test_xlsx_catalogue_importer.py` (new)
- `ai_services/recommendation/tests/unit/application/test_catalogue_audit_service.py`
  (new)
- `ai_services/recommendation/pyproject.toml` (added `openpyxl`,
  `types-openpyxl` dependencies)
- `ai_services/work_packets/REC-R01-01.md`

## Do not change

- `ai_services/recommendation/src/curalina_recommendation/adapters/fake_catalogue_importer.py`
  (left untouched — still the adapter every other caller uses; this
  packet adds a second, real adapter alongside it, per the port being a
  structural `Protocol`)
- `ai_services/recommendation/src/curalina_recommendation/domain/**`,
  `ports/**`, `api/**`, `bootstrap.py`, `settings.py` (A2's frozen
  surface; no domain contract changed)
- `agentic_flow/**`, `architecture/adr/**` (ADR-0005 is not edited by this
  packet; `agentic_flow/open_questions.yaml` is explicitly not touched —
  OQ-011 is `owner: client`, per the ADR's own consequence note)
- Other service packages

## Deliverables

- Contract fixtures: none (no HTTP contract touched).
- Domain/application code:
  - `application/catalogue_audit_service.py` — `audit_workbook`,
    `compare_vocabulary_to_mapper`, `read_mapper_canonical_vocabulary`,
    `oq009_attribute_presence`, `serialize_product_sample`.
- API/worker code: none.
- Adapter code:
  - `adapters/xlsx_parsing.py` — column-index constants and pure
    cell-parsing helpers (both supplier dimension-string formats, the
    primary-category rule, `Decimal`-safe money parsing, junk-category and
    datetime-cell predicates, the `XXX` "unconfirmed currency" sentinel).
  - `adapters/xlsx_workbook_reader.py` — the one place `openpyxl` touches
    disk; returns plain tuples, never a `DataFrame`.
  - `adapters/xlsx_catalogue_importer.py` — `XlsxCatalogueImporter`, the
    **real** `CatalogueImporter` implementation (workflow step 2, A2),
    building one `CatalogueSnapshot` per supplier.
- Tests: `tests/unit/adapters/test_xlsx_parsing.py` (12 tests),
  `tests/unit/adapters/test_xlsx_catalogue_importer.py` (11 tests),
  `tests/unit/application/test_catalogue_audit_service.py` (14 tests) — 37
  new tests, all offline/synthetic fixtures (none read `attached_assets`;
  the real file is exercised only by the notebook itself).
- Docs/report/ADR: `architecture/notebooks/recommendation/01_catalogue_audit.ipynb`
  (filled and executed, 23 cells, all 7 standard sections), this packet.

## Local commands

Required before completion:

```bash
cd ai_services/recommendation
make test
make test-contract
make test-integration
make lint
make typecheck
```

Notebook execution (no `make` target exists for notebooks in this repo;
run via `nbclient` against the `curalina-py314` kernel, which has the
service installed editable — the plain `python3` kernel does not):

```bash
cd architecture/notebooks/recommendation
python3 -c "
import nbformat
from nbclient import NotebookClient
nb = nbformat.read('01_catalogue_audit.ipynb', as_version=4)
NotebookClient(nb, timeout=120, kernel_name='curalina-py314').execute()
nbformat.write(nb, '01_catalogue_audit.ipynb')
"
```

Optional slow commands: none.

## Acceptance criteria

- Every ADR-0005 factual claim in its "Checks that verify this ADR's
  factual claims" list is independently re-derived from the pinned file
  by package code the notebook calls, not restated from the ADR's text.
- The notebook's decision record carries ADR-0005's provenance caveat
  in substance (verbatim block reproduced), plus the caveat's
  consequences (no importer default may be derived from this file's
  values; OQ-011 stays open; OQ-009 untouched; no bearing on the photo
  blocker) — not a single passing mention.
- The decision record does not claim R03/composition readiness; it states
  "accept for catalogue-audit purposes; R03 composition remains blocked
  per ADR-0005."
- Every notebook cell is either narrative, a plot, or a call into
  `curalina_recommendation` package code — no loop over rows, no inline
  parsing regex, no ad-hoc `openpyxl`/`pandas` call lives in a cell body.
- The real `XlsxCatalogueImporter` rejects malformed rows with a named
  reason (`missing_category`, `invalid_sku`, `duplicate_product_key`)
  rather than raising or silently dropping them.
- `make test`, `make lint`, `make typecheck` are clean for the whole
  `recommendation` service after adding the new dependency.

## Known blockers

- None blocking this packet's own completion. R03 remains blocked by
  ADR-0005 (zero rugs/lighting/accent chairs, no availability data) —
  this is recorded as the expected, correct outcome, not a blocker to
  resolve here.
- OQ-009 (attribute completeness) is untouched, confirmed absent both as
  a workbook header and as a mapper-vocabulary term, `owner:
  design_authority`, still `blocking`.

## Completion evidence

**Independent verification against the pinned file** (re-derived by
`audit_workbook`/`XlsxCatalogueImporter`, not restated from ADR-0005):

| Claim | ADR-0005 | This run |
|---|---|---|
| Rows x columns | 385 x 36 | 385 x 36 (confirmed) |
| Supplier split | Four Hands 271, Moe's Home 114 | 271 / 114 (confirmed) |
| Distinct `(supplier, sku)` pairs | ~343 | 343 (confirmed) |
| Duplicate compound keys | 42, all Moe's Home | 42, all Moe's Home (confirmed) |
| `Source File` mislabels | 42 Moe's rows -> a Four Hands sheet | 42 (confirmed, after excluding the genuinely mixed `Four Hands_Moes Home Beds.xlsx` sheet from the mismatch count) |
| Distinct `Source File` values | 11 | 11 (confirmed) |
| Junk category row | 1 (`Final CSV-Ready Summary`, `ER-1073-03`) | 1, same SKU (confirmed) |
| `Coffee Table` rows | 0 | 0 (confirmed) |
| Dimension-string parse coverage | 385/385 | 385/385 (confirmed) |
| Delivery columns empty | 0/385 populated (all 3 columns) | confirmed |
| `LEAD Time` datetime-typed | 65/385 | 65/385 populated, 65/65 of those datetime-typed (confirmed) |
| OQ-009 attributes present | 0/6, neither header nor mapper vocab | 0/6 (confirmed) |
| **Blank `Furniture Category` rows** | **2** | **0 — did not reproduce; corrected in the notebook, not restated** |

The blank-category count is the one ADR-0005 claim that did not
reproduce. It does not change any other finding or the decision below,
and does not affect the primary-category rule's recommendation (there is
simply nothing to apply it to here), but it is recorded as a correction
in the notebook's failure-analysis section (Section 5) and decision
record (Section 6), per this packet's own standard of independent
verification rather than restatement.

**Structural mismatch resolution** (ADR-0005 flagged, asked R01 to
resolve): `CatalogueSnapshot.supplier_id` is singular; the workbook merges
two suppliers into one sheet. Resolved as **one snapshot per supplier**,
via two `import_catalogue` calls — `import_catalogue` already takes
`supplier_id`, so no change to `CatalogueSnapshot` was needed. Both
snapshots were actually built in the notebook (`snap_3ad1f5d7_four-hands`,
271 imported, 0 rejected; `snap_3ad1f5d7_moes-home`, 72 imported, 42
rejected as `duplicate_product_key`), not merely proposed.

**Decision record content** (Section 6 of the notebook, in full):

- ADR-0005's provenance caveat, reproduced verbatim.
- "Accept for catalogue-audit purposes. ... **R03 composition remains
  blocked, per ADR-0005 — this run does not change that.**" (exact
  wording used, to avoid any overclaim).
- Explicit recommendations, marked as recommendations rather than
  resolved facts: primary-category rule (first comma token), currency
  sentinel `XXX` (ISO 4217 "no currency", not a guessed USD/CAD),
  `Availability.UNKNOWN` for all 385 rows, `product_id` minting scheme
  (`f"{slug(supplier_id)}:{normalized_sku}"`).
- Explicit statement that no importer default/threshold/business rule may
  be derived from this file's specific values until the client confirms
  ADR-0005's five provenance points.

Commands run and their real results (from `ai_services/recommendation/`,
`python3` — the shell has no `python`):

```
$ make test
...
TOTAL                                                                  906     64    150     18    92%
85 passed in 1.59s
```

(48 pre-existing + 37 new: 12 in `test_xlsx_parsing.py`, 11 in
`test_xlsx_catalogue_importer.py`, 14 in `test_catalogue_audit_service.py`.)
New modules' own coverage: `xlsx_catalogue_importer.py` 98%,
`xlsx_parsing.py` 96%, `xlsx_workbook_reader.py` 94%,
`catalogue_audit_service.py` 94% branch coverage.

```
$ make test-contract
14 passed, 2 warnings in 0.63s

$ make test-integration
1 passed in 0.16s
```

Unchanged from before this packet — confirms nothing outside the allowed
files regressed.

```
$ make lint
python3 -m ruff check --no-cache .
All checks passed!

$ make typecheck
python3 -m mypy --strict --cache-dir=/tmp/curalina_recommendation_mypy_cache src/curalina_recommendation
Success: no issues found in 33 source files
```

**Dependency change:** `openpyxl>=3.1` added as a runtime dependency
(this is the first code in the service that reads a workbook);
`types-openpyxl>=3.1` added to `dev` for `mypy --strict`. `requirements.lock`
was not touched — it is an explicit A0 placeholder ("a repository-wide
lock policy has not been selected"), unaffected by this phase. Full
`make test`/`make lint`/`make typecheck` were re-run for the whole
service after adding the dependency (all three above), per this task's
explicit instruction not to add a cross-package dependency without
re-verifying the whole suite.

**Notebook execution:** executed cleanly end to end (0 error outputs
across 23 cells) via the `curalina-py314` Jupyter kernel (the only local
kernel with `curalina-recommendation` installed editable — the default
`python3` kernel raised `ModuleNotFoundError`, documented in the Local
commands section above so a re-run does not repeat that trap). Run
artifacts (`manifest.json`, `import_report.json`, `canonical_sample.json`)
were written to `architecture/notebooks/recommendation/runs/R01_<run_id>/`
and are local/ignored, not committed.

**Limitations / follow-up:**

- The blank-category discrepancy against ADR-0005 (2 claimed, 0 found)
  should be resolved by whoever owns ADR-0005 next — either the ADR is
  corrected, or a version difference between what the ADR's author
  inspected and the currently-pinned file is identified. This packet does
  not have authority to edit the ADR.
- This packet does not touch `agentic_flow/open_questions.yaml` — per
  ADR-0005's own consequence note, OQ-011 is `owner: client` and stays
  `open` until the client's five-point confirmation arrives.
- R02 (ranking baselines) and R03 (bundle/substitutions) are unaffected
  and unattempted by this packet; R03 specifically remains blocked by
  ADR-0005 regardless of R01 clearing.
- No hypothesis property tests were added; none were named as required
  for R01 by the notebook standard or ADR-0005.
