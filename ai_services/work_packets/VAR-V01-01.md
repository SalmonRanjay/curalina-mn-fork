# Agent work packet

## Task

Service: `curalina_variants`
Phase: V01 — masks and colour baseline notebook
Owner: `ml-notebook-engineer`
Reviewer: `ai-ml-lead` (evidence-quality sign-off; this packet produces
evidence, not a verdict — `ai-ml-lead` decides whether it clears any gate)

## Objective

Fill and execute `architecture/notebooks/variant_generator/01_masks_and_colour.ipynb`
against the real ATRIANI supplier corpus, per `ADR-0012` ("V01 reference-free
evaluation design") **in full** — §D2 metric set, §D5 pair admission, §D6
controls, §D8 engineering constraints, §D9 claim ceiling — and `ADR-0008`
for corpus admission conditions (superseded figures per `ADR-0012`).

## Context

Required reading, in order:

- `agent_instructions/STATUS.md` operational reference (dispatch item 9)
- `architecture/adr/ADR-0012-v01-reference-free-evaluation-design.md` — binding
- `architecture/adr/ADR-0008-supplier-image-corpus-provenance-and-v01-admissibility.md`
  — corpus admission conditions; its corpus figures (163/41) are superseded
  by `ADR-0012`'s (163/40, 95/26 full-product)
- `agentic_flow/16_notebook_standard.md` — the 7-section structure
- `ai_services/recommendation/src/curalina_recommendation/evaluation/` — the
  established vocabulary/pool/freeze/metrics package-separation pattern

Input data:

- `/Users/rjsalmon/Downloads/Supplier Images/ATRIANI` (out-of-repository,
  not copied in; notebook-scope `CURALINA_SOURCE_ASSETS_DIR` only)
- `ATRIANI/Design 44.xlsx`, sha256
  `f4b00dce359dbf19d5a0abad9eba56ecdadaf3f1490116bcfb83d9e69872c0bb` — the
  allowlist source (`Linl to pictures ` column, sic)

## Allowed files

- `architecture/notebooks/variant_generator/01_masks_and_colour.ipynb`
- `architecture/notebooks/variant_generator/runs/**` (new run artifacts)
- `ai_services/variant_generator/src/curalina_variants/evaluation/corpus.py` (new)
- `ai_services/variant_generator/src/curalina_variants/evaluation/pair_admission.py` (new)
- `ai_services/variant_generator/src/curalina_variants/evaluation/human_masks.py` (new)
- `ai_services/variant_generator/src/curalina_variants/evaluation/predictor.py` (new)
- `ai_services/variant_generator/src/curalina_variants/evaluation/image_io.py` (new;
  factored out of `adapters/lab_colour_transfer.py`, behaviour-preserving)
- `ai_services/variant_generator/src/curalina_variants/adapters/identity_colour_transfer.py`
  (new; control-only, never wired into a bootstrap)
- `ai_services/variant_generator/src/curalina_variants/adapters/lab_colour_transfer.py`
  (import-only edit: use the new `image_io` module instead of private
  duplicated codec helpers; no behaviour change)
- `ai_services/variant_generator/tests/unit/test_corpus.py`,
  `test_pair_admission.py`, `test_human_masks.py`, `test_predictor.py`,
  `test_image_io.py`, `test_identity_colour_transfer.py` (new)
- `ai_services/variant_generator/pyproject.toml` (added `openpyxl>=3.1`
  runtime dependency and `types-openpyxl>=3.1` dev dependency, mirroring
  recommendation's precedent for the same library)
- `ai_services/work_packets/VAR-V01-01.md`

## Do not change

- `Settings` (`ai_services/variant_generator/src/curalina_variants/settings.py`)
  — no `CURALINA_SOURCE_ASSETS_DIR` field added, per `ADR-0012` §D8.5.
- `agentic_flow/open_questions.yaml` — not touched.
- Any ADR.
- `ports/colour_transfer.py`'s contract — unchanged; `LabColourTransferAdapter`
  and the new `IdentityColourTransferAdapter` both implement the existing
  signature.
- `api/**`, `bootstrap.py`, `workers/**` — `IdentityColourTransferAdapter` is
  control-only and is not wired into any composition root.
- Other service packages.

## Deliverables

- `evaluation/image_io.py` — `decode_rgb`/`encode_png`, factored out of
  `adapters/lab_colour_transfer.py` so the identity control adapter does not
  duplicate codec code.
- `evaluation/corpus.py` — ATRIANI allowlist loading from `Design 44.xlsx`'s
  `Linl to pictures ` column (never a glob), the `ADR-0008` name-exclusion
  regex/thumbnail rule, and `compute_funnel` reporting counts at every
  admission stage per `ADR-0012` §D7.
- `evaluation/pair_admission.py` — all four `ADR-0012` §D5 pair-admission
  conditions (do-nothing Delta-E00 >= 15 floor, white-border/backdrop
  confirmation, human product confirmation, swatch central-crop
  disagreement <= 5), plus `mean_lab`/`region_mean_lab`/`central_crop`
  target-colour-extraction helpers (mean-in-LAB, the rule `ADR-0012` §D5
  pins).
- `evaluation/human_masks.py` — `rectangles_to_mask`, turning human-declared
  editable rectangles into a `Mask` with no segmentation and no derivation
  from pixel content, per `ADR-0012` §D8.6.
- `evaluation/predictor.py` — `dark_to_light_risk` (the technical design's
  formula, implemented only so it can be re-tested) and `pearson_correlation`.
- `adapters/identity_colour_transfer.py` — `IdentityColourTransferAdapter`,
  the C1 no-op control, computing diagnostics through the real
  `evaluate_transfer_metrics` path.
- The executed notebook (real outputs, real ATRIANI corpus, 40 cells, 0
  error outputs) plus one archived run directory under
  `architecture/notebooks/variant_generator/runs/V01_<timestamp>/`.

## Local commands

Required before completion:

```bash
cd ai_services/variant_generator
make test
make test-contract
make lint
make typecheck
```

Notebook execution (`curalina-py314` kernel, which has the service installed
editable; `matplotlib`/`pandas` needed for plotting/presentation only, never
for business logic):

```bash
cd architecture/notebooks/variant_generator
python3 -m jupyter nbconvert --to notebook --execute --inplace \
  --ExecutePreprocessor.kernel_name=curalina-py314 \
  --ExecutePreprocessor.timeout=300 01_masks_and_colour.ipynb
```

## Acceptance criteria

- Corpus funnel independently reproduced by `compute_funnel`, matching
  `ADR-0012`'s recorded figures exactly (42 folders allowlisted, 40 with an
  admitted `.webp`, 197 webp seen, 34 excluded by name, 163 admitted) —
  asserted in the notebook, not merely printed.
- Pair admission enforces all four `ADR-0012` §D5 conditions in code; every
  rejection reported with its reason, never dropped silently.
- M1 reported as `assertion_held`, never a score, never in a pass rate. M2a
  (>= 0.90) and M2b (<= 0.02, labelled an operating point everywhere it
  appears) are primary. M3 reported as a distribution only, no tolerance
  applied. M4 reported unthresholded.
- All three `ADR-0012` §D6 controls run and their required outcomes checked
  in code (not asserted by citation): C1 identity (M1/M2/M4 trivial pass, M3
  fails at do-nothing distance), C2 non-product
  (`Bench J/BZ 28 EL-MALIBU-VELVET-ICE.webp` scores at/near ceiling), C3
  wrong-target (M2/M4 indistinguishable from the correct-target run).
- The `dark_to_light_risk` vs `source_mask_l_std` failure-predictor
  comparison is computed on this run's own admitted pairs, not cited from
  `ADR-0012`'s probe, with the small-sample caveat stated explicitly.
- Decision record states one of `ADR-0012` §D9's supported outcomes, copies
  the claim ceiling in substance, and the word "held-out" appears nowhere in
  the notebook or its run artifacts (grep-verified).
- Per-module coverage reported for every new module (not just the
  aggregate); `make test`/`make test-contract`/`make lint`/`make typecheck`
  clean for the whole service.

## Known blockers

- **The Delta-E tolerance remains `needs_input`** (`ADR-0012` §D4) — this
  run does not set it, cites the swatch intra-file Delta-E00 p90 (8.06) only
  as prior context, and applies no pass/fail tolerance to M3 anywhere.
- **n=4 admitted pairs.** Only 8 folders were human-reviewed for this first
  run (out of 40 folders with admitted images); 4 of the 8 candidate pairs
  were rejected at admission, all for the same reason (do-nothing distance
  below the 15 Delta-E00 floor — the manifest-matched swatch was already
  close to the product's real colour). This is `ADR-0012` §D5.1 working as
  designed, not a data defect, but it makes the failure-predictor
  correlation and the M2 pass-rate small-sample results, stated as such.
- **7 of 8 target swatches matched the manifest's `Finish` text** on visual
  inspection during pair selection — far above the corpus-wide 2-of-42 rate
  `ADR-0008` measured. This run's swatch-match rate is a property of a
  curated selection, not representative of the corpus, and is stated as a
  limitation rather than presented as a corpus-wide finding.
- **Editable-region masks are hand-authored approximate rectangles** from
  visual inspection of each image (upholstery only, legs/hardware/background
  excluded), not pixel-precise segmentations — satisfying `ADR-0012` §D8.6's
  "not background-derived or auto-segmented" requirement, but at lower
  spatial precision than a designer-drawn mask would have.
- LUXUS is untouched, per `ADR-0012`/`ADR-0008` — reserved for a second run.

## Completion evidence

**Package/test verification**, run from `ai_services/variant_generator/`:

```
$ make test
120 passed in 1.89s
TOTAL   1291 stmts   82 miss   216 branch   31 partial   92%

$ make test-contract
27 passed, 2 warnings in 0.71s

$ make lint
python3 -m ruff check --no-cache .
All checks passed!

$ make typecheck
python3 -m mypy --strict --cache-dir=/tmp/curalina_variants_mypy_cache src/curalina_variants
Success: no issues found in 39 source files
```

**Per-module coverage for every new/changed module** (from `make test`'s
`pytest-cov` report):

| Module | Stmts | Branch | Coverage |
|---|---|---|---|
| `evaluation/image_io.py` | 12 | 0 | 100% |
| `evaluation/corpus.py` | 70 | 18 | 98% (one line, a short-row `continue` guard, exercised in effect by `test_load_allowlisted_folder_names_skips_short_rows` but not on the exact branch counter) |
| `evaluation/pair_admission.py` | 85 | 22 | 100% |
| `evaluation/human_masks.py` | 16 | 8 | 100% |
| `evaluation/predictor.py` | 16 | 6 | 100% |
| `adapters/identity_colour_transfer.py` | 19 | 2 | 100% |
| `adapters/lab_colour_transfer.py` | 25 | 2 | 100% (unchanged after the `image_io` refactor) |

No new module sits at 0%. Overall service coverage moved 91% -> 92%
(1272 -> 1291 statements) with the new modules added.

**Pre-existing, unrelated finding** (re-verified, not caused by this
packet): `make test-integration` still fails
`test_cancellation_after_completion_read_cannot_resurrect_job`, the known
`VAR-A3-01` regression recorded in `agent_instructions/STATUS.md` item 2.
This packet touches no integration test and no `api/`/`workers/` file.

**Notebook execution**: executed via
`jupyter nbconvert --execute --inplace --ExecutePreprocessor.kernel_name=curalina-py314`
against the real ATRIANI corpus — 40 cells, **0 error outputs**. Run
artifacts archived at
`architecture/notebooks/variant_generator/runs/V01_20260915T043322Z/`
(`manifest.json`, `decision.json`, `pair_admission.csv`,
`primary_metrics.csv`, `rejected_pairs.csv`, `control_c1_identity.csv`,
`control_c3_wrong_target.csv`, `predictor_comparison.csv`,
`asset_hashes.csv`, `m2_per_pair.png`). `grep -ric "held-out\|held out"` on
the notebook file and every run artifact returns `0` everywhere.

**Corpus funnel** (independently reproduced by `compute_funnel`, asserted
equal to `ADR-0012`'s recorded figures): 42 folders allowlisted from
`Design 44.xlsx`'s `Linl to pictures ` column → 40 with >=1 admitted `.webp`
(`Curva King Bed` and `Zuma Side Table` both drop entirely) → 197 `.webp`
seen → 34 excluded by name (2 named contaminants + 32 `-150x150`
thumbnails) → **163 admitted**. Workbook sha256:
`f4b00dce359dbf19d5a0abad9eba56ecdadaf3f1490116bcfb83d9e69872c0bb`.

**8 candidate pairs reviewed, 4 admitted, 4 rejected** — every rejection
reported, none dropped:

| pair_id | do-nothing ΔE00 | admitted | rejection reason |
|---|---|---|---|
| artigiano_dining_chair | 51.06 | yes | — |
| vela_arm_chair | 11.53 | no | `do_nothing_delta_e_below_floor` |
| forma_sofa | 33.56 | yes | — |
| alice_chair | 27.04 | yes | — |
| cielo_accent_chair | 8.90 | no | `do_nothing_delta_e_below_floor` |
| abbraccio_armchair | 6.53 | no | `do_nothing_delta_e_below_floor` |
| poltrona_chair_studio | 16.10 | yes | — |
| mia_chair | 5.86 | no | `do_nothing_delta_e_below_floor` |

**M1 (assertion)**: `assertion_held = True` on all 4 admitted pairs (0
changed pixels outside the mask in every case) — reported as an assertion,
never a score.

**M2a / M2b per admitted pair (primary)**:

| pair_id | M2a (L\* var. ratio) | M2a >= 0.90 | M2b (clipped fraction) | M2b <= 0.02 |
|---|---|---|---|---|
| artigiano_dining_chair | 0.9434 | pass | 0.3471 | **fail** |
| forma_sofa | 0.9997 | pass | 0.0022 | pass |
| alice_chair | 0.9039 | pass | 0.6146 | **fail** |
| poltrona_chair_studio | 0.9925 | pass | 0.1670 | **fail** |

M2a pass rate 4/4 (1.00); M2b pass rate 1/4 (0.25). The two most extreme
M2b failures (`alice_chair` 0.61, `artigiano_dining_chair` 0.35) are both
light-source-region-to-dark-target requests with the widest
`source_mask_l_std` (21.5, 14.9) — consistent with `ADR-0012` Finding 5's
chroma-against-shading-range mechanism, re-derived on this run rather than
assumed.

**M3 (demoted, no tolerance applied)**: distribution across the 4 admitted
pairs — 15.05, 20.57, 32.89, 50.38 (min/median/max: 15.05 / 26.64 / 50.38).
Reported as a distribution only.

**M4 (unthresholded)**: 0.9581, 1.0, 1.0, 0.9998 — reported for instrument
calibration only; V02's 0.98 bar not applied.

**Controls (`ADR-0012` §D6), all confirmed on this run's own pairs**:

- **C1 identity**: M1/M2a/M2b/M4 pass trivially (M2a=1.0, M2b=0.0, IoU=1.0)
  on all 4 admitted pairs' identity re-runs; M3 fails at the do-nothing
  distance on every one. Confirmed in code, printed:
  *"M1, M2 and M4 cannot distinguish 'correctly transferred' from 'did
  nothing' — only M3, and only because D5.1 enforced do-nothing headroom,
  separates them."*
- **C2 non-product**: `Bench J/BZ 28 EL-MALIBU-VELVET-ICE.webp` under a
  full-frame mask and a self-extracted target scored M2a=0.9363, M2b=0.0,
  Delta-E00=0.25, IoU=1.0 — at/near ceiling on every metric, reproducing
  `ADR-0012` Finding 6 independently. Confirmed: *"the metric set cannot
  detect that this input is a material swatch, not a product photograph."*
- **C3 wrong-target**: rotating targets among the 4 admitted pairs, the
  largest |correct − wrong| gap was M2a=0.1433, M4=0.0861 — both
  indistinguishable in practice from run-to-run noise. Confirmed: *"M2/M4
  cannot tell a correct target from a wrong one."*

**Predictor replacement test** (`ADR-0012` Finding 5, re-tested on this
run's own 4 pairs, not cited): `dark_to_light_risk` was **0.0 on all 4**
admitted pairs — every requested target here is darker than its source
region, which the formula floors to zero risk regardless of actual observed
clipping (0.35, 0.002, 0.61, 0.17); the correlation is undefined
(zero-variance input), which is itself a stronger finding than a weak
correlation would have been. `corr(gamut_clipped_fraction, source_mask_l_std)
= 0.607` — same direction as `ADR-0012`'s probe (+0.720), but **n=4 cannot
certify a coefficient**; reported as a small-sample replication attempt.

**Decision record (Section 6 of the notebook, in full)**: recorded outcome
is **`revise`** — the failure predictor should move from `dark_to_light_risk`
to a mask-region `std(L*)` probe, pending a larger-n confirmation; this is
evidence relevant to a future V02 go/no-go call, not a go/no-go decision
itself. `ADR-0012` §D9's claim ceiling is copied into the decision record
in substance verbatim (see below).

**Claim ceiling — copied verbatim in substance into the decision record**:

Supports: the LAB baseline and `hard_composite` behave as specified on 4
human-reviewed ATRIANI product renders (M1 holds exactly; M2 quantified per
pair); a revise signal on the failure predictor; evidence relevant to a
future V02 go/no-go call, not the call itself.

Does not support: no G-series stage gate; no camera-photography claim
(CGI renders — uniform lighting, no sensor noise, geometrically perfect
edges); no colour-accuracy claim against a true variant (no reference exists
in this corpus — the words "colour accuracy", "fidelity", "matches the
variant", "ground truth" do not apply); no "synthetic-free corpus" claim; no
Delta-E tolerance claim; no generalisation claim beyond ATRIANI (a
development-set result; a second supplier is reserved for a follow-up run
and was not touched); no movement on V02, V03, G01, `OQ-009`, `OQ-010` or
`OQ-011`.

**No `ai-ml-lead` sign-off is recorded here** — this packet delivers
evidence per the acceptance criteria above; the gate verdict is
`ai-ml-lead`'s to make.
