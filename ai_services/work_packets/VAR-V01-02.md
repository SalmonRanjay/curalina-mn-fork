# Agent work packet

## Task

Service: `curalina_variants`
Phase: V01 correction — record/reporting fixes on the already-run notebook
(no new pair admission, no new computed metric values)
Owner: `ml-notebook-engineer`
Reviewer: `ai-ml-lead` (this packet does not attempt sign-off; it closes the
four record-integrity defects and two under-deliveries `ai-ml-lead` listed
against `VAR-V01-01` / run `V01_20260915T043322Z`)

## Objective

Fix exactly the four defects and two under-deliveries recorded in
`agent_instructions/STATUS.md`'s "Item 9" review (2026-09-15), re-execute
`architecture/notebooks/variant_generator/01_masks_and_colour.ipynb` fresh so
source/output/`decision.json` all agree, and archive a new run directory.
**No new pairs admitted or rejected; no computed metric value (M1/M2a/M2b/
M3/M4, correlations) changed** — this is a correction to what the record
says about those numbers, not the numbers themselves. The prior run
directory `runs/V01_20260915T043322Z/` is left in place, superseded not
deleted.

## Context

Required reading, in order:

- `agent_instructions/STATUS.md` "Item 9" section — the full withheld
  sign-off review naming all four defects and two under-deliveries verbatim
- `architecture/adr/ADR-0012-v01-reference-free-evaluation-design.md` — the
  binding metric set, pair admission rule (§D5, including §D5.4), controls
  (§D6), reporting rule (§D7), and claim ceiling (§D9) this correction must
  keep honoring
- `ai_services/work_packets/VAR-V01-01.md` — the original packet and its
  completion evidence, for the numbers that must not move

## Allowed files

- `architecture/notebooks/variant_generator/01_masks_and_colour.ipynb`
  (cells 6, 11, 13, 27, 35, 37 source corrected; one new narrative markdown
  cell inserted after the pair-admission table; re-executed fresh)
- `architecture/notebooks/variant_generator/runs/**` (new run directory only;
  `V01_20260915T043322Z/` untouched)
- `ai_services/variant_generator/src/curalina_variants/evaluation/pair_admission.py`
  (added `intra_file_delta_e00_p90`, per `ADR-0012` §D5.4's second half)
- `ai_services/variant_generator/tests/unit/test_pair_admission.py` (tests
  for the new function)
- `ai_services/variant_generator/tests/unit/test_transfer_metrics.py`
  (closed `metrics.py`'s three uncovered validation branches)
- `ai_services/design_rules/tests/fixtures/golden_scenarios.py` (module
  docstring only — reflects `ADR-0009`/`ADR-0014`, no scenario/fixture
  content changed)
- `ai_services/work_packets/VAR-V01-02.md` (this file)

## Do not change

- Pair admission: the 4 admitted / 4 rejected pairs from
  `V01_20260915T043322Z` stand exactly as-is. No new folder reviewed, no
  admission condition altered.
- Any computed metric value: M1/M2a/M2b/M3/M4, `source_mask_l_std`,
  `dark_to_light_risk`, correlations, C1/C2/C3 control outputs — all
  independently re-verified byte-identical to the prior run's CSVs.
- `runs/V01_20260915T043322Z/` — left in place.
- `agentic_flow/open_questions.yaml`, any ADR.

## Deliverables — the four defects

1. **Cell 37's source** (the `decision.json`-building cell) no longer
   contains "M2a/M2b pass on 2 of 4 admitted pairs and fail together on the
   other 2". Corrected to: M2a passes on all 4 admitted pairs; M2b passes
   on only 1 of 4 (`forma_sofa`); 1 of 4 passes both; the remaining 3
   (`artigiano_dining_chair`, `alice_chair`, `poltrona_chair_studio`) fail
   specifically on M2b. Notebook re-executed fresh — source, stored output,
   and the newly archived `decision.json` all now agree (verified by
   diffing the cell's printed JSON against the archived file).
2. **Cell 35's markdown source** corrected to name all three M2b failures
   (`artigiano_dining_chair` 0.3471, `alice_chair` 0.6146,
   `poltrona_chair_studio` 0.1670) and the one pair passing both M2a and
   M2b (`forma_sofa` 0.0022) — verified against cell 34's own printed count
   ("3 of 4") before writing.
3. **The failure-mechanism narrative in cell 35 and `decision.json`
   corrected.** No longer claims light-to-dark direction or lightness
   spread as a confirmed mechanism. States plainly: `dark_to_light_risk` is
   exactly `0.0` on all four admitted pairs including the pass (zero
   discriminative range — a stronger falsification than a low correlation);
   `source_mask_l_std`'s correlation with clipping (+0.6068) is in the same
   direction as `ADR-0012`'s probe (+0.720) but is carried largely by one
   pair (`alice_chair`) at n=4, so it is a **candidate to test at larger n**,
   not a confirmed replacement predictor.
4. **Cell 27/28 (the C3 control cell) now computes and prints the M2b gap**
   alongside M2a/M4, and the printed conclusion is corrected: M2a and M4
   confirm `ADR-0012`'s expected outcome (indistinguishable between correct
   and wrong target); M2b does not — it flips pass/fail in both directions
   (`forma_sofa`: correct 0.0022 PASS vs wrong-target 0.3627 FAIL, gap
   0.3605; `alice_chair`: correct 0.6146 FAIL vs wrong-target 0.0035 PASS,
   gap 0.6111). Stated as the C3 finding being **strengthened, not
   weakened**: M2b is strongly target-dependent and 0.02 must not be
   treated as a stable, target-independent operating point without more
   evidence. `decision.json`'s `C3_wrong_target` field carries the same
   correction.

## Deliverables — the two under-deliveries

- **§D5.4's second half.** Added
  `evaluation.pair_admission.intra_file_delta_e00_p90(rgb) -> float`
  (per-pixel CIEDE2000 vs the file's own mean LAB, p90), covered by two new
  unit tests. The notebook's pair-admission cell (11) now computes it for
  every candidate pair's swatch file and cell 13 writes both
  `swatch_disagreement_delta_e00` (already computed but previously dropped
  from output) and the new `swatch_intra_file_delta_e00_p90` into
  `pair_admission.csv` for every pair, admitted or not. A new narrative
  cell states this run's own swatch p90s are a distinct, freshly-computed
  number, not a reuse of `ADR-0012`'s corpus-wide 8.06 figure (which
  describes a different 113-file set and is cited only as precedent for the
  kind of number this is).
- **§D7's funnel table.** Cell 6's markdown now states explicitly that of
  the 163 name-admitted files, 19 are material/finish samples (not
  products) and only 95 are full-product white-background images across 26
  of 40 folders — and that this run's 8 folders / 4 admitted pairs are a
  small hand-picked slice of that larger admitted corpus, not a
  corpus-wide or random sample. The same two figures are carried into
  `decision.json`'s `sample_size` and `known_limitations`.

## Deliverables — `metrics.py` coverage

Added four tests to `tests/unit/test_transfer_metrics.py` exercising the
three previously-uncovered `ValueError` branches in
`evaluate_transfer_metrics` (`metrics.py:41,43,45` — bad `ndim`, wrong
channel count, `editable_mask` shape mismatch, `clipped_mask` shape
mismatch). `evaluation/metrics.py` moved from 93% to **100%** (67/67
statements, 20/20 branches).

## Deliverables — `golden_scenarios.py` docstring (nice-to-have, done)

Module docstring updated to reflect `ADR-0009` (Design Manual located,
admitted read-in-place by hash) and `ADR-0014` §D4 (§8 ruled inapplicable
to spatial scope / inadmissible as an evaluation set) instead of the stale
"not yet supplied" framing. No fixture data or scenario content changed.

## Local commands

```bash
cd ai_services/variant_generator
make test
make test-contract
make test-integration
make test-worker
make lint
make typecheck

cd ../design_rules
make test
```

Notebook re-execution (`curalina-py314` kernel):

```bash
cd architecture/notebooks/variant_generator
CURALINA_SOURCE_ASSETS_DIR="/Users/rjsalmon/Downloads/Supplier Images" \
  python3 -m jupyter nbconvert --to notebook --execute --inplace \
  --ExecutePreprocessor.kernel_name=curalina-py314 \
  01_masks_and_colour.ipynb
```

## Completion evidence

**Package verification, `ai_services/variant_generator/`:**

```
$ make test
126 passed, 2 warnings in 1.94s
TOTAL   1296 stmts   79 miss   216 branch   28 partial   92%
evaluation/metrics.py           67      0     20      0   100%
evaluation/pair_admission.py    90      0     22      0   100%

$ make test-contract
27 passed, 2 warnings in 0.64s

$ make test-integration
6 passed, 1 failed in 1.10s
FAILED test_cancellation_after_completion_read_cannot_resurrect_job
```

The one integration failure is the pre-existing `VAR-A3-01` regression
(cancellation racing completion) recorded in `agent_instructions/STATUS.md`
item 2, independently re-verified as unrelated to this packet — this packet
touched no `api/`, `workers/`, or integration test file.

```
$ make test-worker
2 passed in 0.50s

$ make lint
All checks passed!

$ make typecheck
Success: no issues found in 39 source files
```

**`ai_services/design_rules/`:**

```
$ make test
83 passed in 6.46s
```

No regression (matches the 83-test baseline recorded after the D01 re-run).

**Per-module coverage, the two touched modules:**

| Module | Stmts | Branch | Coverage (before -> after) |
|---|---|---|---|
| `evaluation/metrics.py` | 67 | 20 | 93% -> **100%** |
| `evaluation/pair_admission.py` | 85 -> 90 | 22 | 100% -> **100%** (grew by the new function, stayed fully covered) |

**Notebook re-execution**: 21 code cells, sequential execution counts 1-21,
**0 error outputs** (grep-verified via the notebook's own JSON). New run
archived at
`architecture/notebooks/variant_generator/runs/V01_20260915T045739Z/`;
`V01_20260915T043322Z/` left untouched.

**Computed metric values — confirmed unchanged**, diffed field-by-field
against the prior run's CSVs:

| pair_id | M2a | M2b | M3 | M4 |
|---|---|---|---|---|
| artigiano_dining_chair | 0.9434 | 0.3471 | 50.379 | 0.9581 |
| forma_sofa | 0.9997 | 0.0022 | 32.887 | 1.0 |
| alice_chair | 0.9039 | 0.6146 | 20.57 | 1.0 |
| poltrona_chair_studio | 0.9925 | 0.1670 | 15.051 | 0.9998 |

Identical to `V01_20260915T043322Z/primary_metrics.csv`. C1, C3 and
predictor-comparison CSVs are likewise byte-identical to the prior run.
Nothing here changed the underlying science.

**Corrected M2b failure list**: `artigiano_dining_chair`, `alice_chair`,
`poltrona_chair_studio` fail (3 of 4); `forma_sofa` passes M2a and M2b
together (1 of 4). M2a pass rate 4/4; M2b pass rate 1/4.

**Corrected C3 finding**: M2a/M4 confirmed indistinguishable between
correct and wrong target (max gaps 0.1433 / 0.0861). M2b is **not**
indistinguishable — max gap **0.6111**, and the pass/fail verdict flips on
2 of 4 pairs (`forma_sofa` PASS->FAIL, `alice_chair` FAIL->PASS). Recorded
as the C3 finding being strengthened: M2b is strongly target-dependent, and
0.02 should not be treated as a stable, target-independent operating point
without more evidence.

**Swatch intra-file Delta-E00 p90, this run's own 8 swatch files** (now in
`pair_admission.csv`, alongside `swatch_disagreement_delta_e00`):

| pair_id | swatch_disagreement_delta_e00 | swatch_intra_file_delta_e00_p90 |
|---|---|---|
| artigiano_dining_chair | 0.235 | 16.568 |
| vela_arm_chair (rejected) | 0.778 | 21.193 |
| forma_sofa | 0.042 | 8.120 |
| alice_chair | 0.061 | 2.706 |
| cielo_accent_chair (rejected) | 1.075 | 17.813 |
| abbraccio_armchair (rejected) | 0.541 | 7.878 |
| poltrona_chair_studio | 0.537 | 21.932 |
| mia_chair (rejected) | 0.672 | 7.991 |

These are this run's own 8 swatch files, independently computed —
**not** a reuse of `ADR-0012`'s corpus-wide 8.06 figure (a different,
113-file set).

**`decision.json`** (new run) carries all four corrections and both
under-delivery fixes: the corrected rationale text, the corrected
`C3_wrong_target` field, `material_samples_excluded_corpus_wide: 19` and
`full_product_images_available_corpus_wide: 95` added to `sample_size`, and
a `known_limitations` entry naming the swatch p90 obligation.

**Decision outcome unchanged**: `revise`, for the same reasons as
`VAR-V01-01` — this packet corrects the record, not the verdict.

**No `ai-ml-lead` sign-off is recorded here.** This packet closes the four
defects and two under-deliveries `ai-ml-lead` listed; whether that clears
Item 9 is `ai-ml-lead`'s call to make on the corrected record.
