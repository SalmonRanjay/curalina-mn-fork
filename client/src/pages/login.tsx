import AuthPortfolio from "@/components/quiz/AuthPortfolio";

// Consultation p3: sign in and sign up share one "Project Portfolio" page.
export default function Login() {
  return <AuthPortfolio />;
}
